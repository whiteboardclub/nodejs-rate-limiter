import "../src/index";
import "../src/interfaces";

describe("Index files", () => {
  it("should export all the necessary modules", () => {
    // This test simply imports from the index files to ensure they are covered.
    expect(true).toBe(true);
  });
});
